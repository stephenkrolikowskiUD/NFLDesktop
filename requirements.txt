pandas>=2.2
numpy>=1.26
requests>=2.32
pytz>=2024.1
gspread>=6.1
gspread-dataframe>=3.3
google-auth>=2.28
# google-generativeai (the old package) has a different import path
# (google.generativeai) than what picks.py actually uses. MLB's engine uses
# `from google import genai` / `from google.genai import types`, which is the
# google-genai package — matching that here rather than the deprecated one.
google-genai>=0.3

# nflverse data layer.
# nflreadpy is the maintained successor to nfl_data_py (archived Sept 2025).
# It returns Polars, so pyarrow is required for .to_pandas() and for reading
# the direct parquet release assets in the fallback path.
nflreadpy>=0.1.5
pyarrow>=15.0
